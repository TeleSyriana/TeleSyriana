name: Apply Firestore quota hotfix

on:
  push:
    branches: [agent/firestore-quota-hotfix-v1]
    paths:
      - '.github/workflows/apply-firestore-quota-hotfix.yml'

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[quota-patch]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/firestore-quota-hotfix-v1
          fetch-depth: 0

      - name: Apply exact quota patches
        shell: python
        run: |
          from pathlib import Path

          def replace_required(text, old, new, label):
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected exactly 1 match, found {count}")
              return text.replace(old, new, 1)

          # ---------- app-core.js ----------
          p = Path('app-core.js')
          s = p.read_text()

          old = '''function startPresence() {
            if (!currentUser) return;
            subscribePresence();
            updatePresence(true);
            if (presenceTimerId) clearInterval(presenceTimerId);
            presenceTimerId = setInterval(() => updatePresence(false), 30_000);
          }'''
          new = '''function stopPresenceListener() {
            if (!presenceUnsub) return;
            try { presenceUnsub(); } catch {}
            presenceUnsub = null;
          }

          function syncHomeRealtimeListeners(pageId = pageVisibleName()) {
            const homeActive = pageId === "home";

            if (homeActive && canViewOnlineNow(currentUser)) subscribePresence();
            else stopPresenceListener();

            if (homeActive && canViewTeamDashboard(currentUser)) subscribeSupervisorDashboard();
            else if (supUnsub) {
              try { supUnsub(); } catch {}
              supUnsub = null;
            }

            if (homeActive) subscribeIssueCalendar();
            else if (issueCalendarUnsub) {
              try { issueCalendarUnsub(); } catch {}
              issueCalendarUnsub = null;
            }
          }

          function startPresence() {
            if (!currentUser) return;
            syncHomeRealtimeListeners();
            updatePresence(true);
            if (presenceTimerId) clearInterval(presenceTimerId);
            presenceTimerId = setInterval(() => updatePresence(false), 60_000);
          }'''
          s = replace_required(s, old, new, 'app presence lifecycle')

          s = replace_required(
              s,
              'if (role !== "agent") return [{ key: "team", source: base }];',
              'if (role !== "agent") return [{ key: "team", source: query(base, where("status", "in", ["open", "waiting_customer", "waiting_courier", "waiting_supplier", "escalated", "urgent"])) }];',
              'active-only management issue calendar'
          )

          s = replace_required(
              s,
              '  updatePresence(false).catch(() => {});\n  try { translateFeaturePages(getLanguage()); applyPhase21LanguagePolish(getLanguage()); setTimeout(() => applyPhase21LanguagePolish(getLanguage()), 80); } catch {}',
              '  updatePresence(false).catch(() => {});\n  syncHomeRealtimeListeners(pageId);\n  try { translateFeaturePages(getLanguage()); applyPhase21LanguagePolish(getLanguage()); setTimeout(() => applyPhase21LanguagePolish(getLanguage()), 80); } catch {}',
              'page-scoped home realtime listeners'
          )

          p.write_text(s)

          # ---------- messages-core.js ----------
          p = Path('messages-core.js')
          s = p.read_text()

          s = replace_required(
              s,
              '  updateBadgesForRoom(key);\n  ensureUnreadWatcher(key);',
              '  updateBadgesForRoom(key);\n  if (currentUser?.id) ensureUnreadWatcher(key);',
              'do not start unread listeners before login'
          )

          marker = '// ---------------- init ----------------\n'
          helpers = '''function messagesPageIsActive() {
            const page = document.getElementById("page-messages");
            return Boolean(page && !page.classList.contains("hidden"));
          }

          function stopMessagesPageRealtime() {
            try { unsubPresence?.(); } catch {}
            unsubPresence = null;
            try { unsubscribeProfiles?.(); } catch {}
            unsubscribeProfiles = null;
            try { unsubscribeالحالة?.(); } catch {}
            unsubscribeالحالة = null;
            unsubscribeGroupsCloud();
            unsubscribeRecentsCloud();
            presenceCache.clear();
          }

          function syncMessagesPageRealtime() {
            setCurrentUser();
            if (!currentUser?.id || !messagesPageIsActive()) {
              stopMessagesPageRealtime();
              return;
            }
            subscribePresenceSidebar();
            subscribeProfilesSidebar();
            subscribeGroupsCloud();
            subscribeRecentsCloud();
            subscribeالحالةDots();
            applyProfileAvatars();
            applyBirthdayBadges();
          }

          // ---------------- init ----------------
          '''
          s = replace_required(s, marker, helpers, 'messages page realtime helpers')

          s = replace_required(
              s,
              '  setCurrentUser();\n  subscribePresenceSidebar();\n  subscribeProfilesSidebar();\n  applyProfileAvatars();',
              '  setCurrentUser();\n  syncMessagesPageRealtime();',
              'remove hidden initial presence/profile listeners'
          )

          s = replace_required(
              s,
              '  subscribeGroupsCloud();\n  subscribeRecentsCloud();',
              '  syncMessagesPageRealtime();',
              'remove hidden initial groups/recents listeners'
          )

          old_nav = '''  document.querySelectorAll(".nav-link[data-page]").forEach((btn) => {
            btn.addEventListener("click", () => {
              if (btn.dataset.page !== "messages") document.body.classList.remove("chat-open");
            });
          });'''
          new_nav = '''  document.querySelectorAll(".nav-link[data-page]").forEach((btn) => {
            btn.addEventListener("click", () => {
              if (btn.dataset.page !== "messages") {
                document.body.classList.remove("chat-open");
                stopMessagesPageRealtime();
              } else {
                setTimeout(syncMessagesPageRealtime, 0);
              }
            });
          });'''
          s = replace_required(s, old_nav, new_nav, 'messages nav lifecycle')

          s = replace_required(
              s,
              '  subscribeالحالةDots();\n});',
              '  syncMessagesPageRealtime();\n});',
              'remove hidden initial status listener'
          )

          old_user_change = '''  subscribeGroupsCloud();
            subscribeRecentsCloud();
            subscribeالحالةDots();
            subscribeProfilesSidebar();
            applyProfileAvatars();
            applyBirthdayBadges();
            applyProfileAvatars();'''
          new_user_change = '''  syncMessagesPageRealtime();
            if (messagesPageIsActive()) {
              applyProfileAvatars();
              applyBirthdayBadges();
            }'''
          s = replace_required(s, old_user_change, new_user_change, 'messages user-change lifecycle')

          p.write_text(s)

          # ---------- groups-core.js ----------
          p = Path('groups-core.js')
          s = p.read_text()

          old_tail = '''document.addEventListener("DOMContentLoaded", () => {
            hookMemberSearch();
            hookUI();
            subscribeGroupsList();
          });

          window.addEventListener("telesyriana:user-changed", () => {
            applySupervisorVisibility();
            subscribeGroupsList();
          });'''
          new_tail = '''function groupsMessagesPageIsActive() {
            const page = document.getElementById("page-messages");
            return Boolean(page && !page.classList.contains("hidden"));
          }

          function stopGroupsList() {
            try { unsubGroups?.(); } catch {}
            unsubGroups = null;
          }

          function syncGroupsListLifecycle() {
            if (getCurrentUser()?.id && groupsMessagesPageIsActive()) subscribeGroupsList();
            else stopGroupsList();
          }

          document.addEventListener("DOMContentLoaded", () => {
            hookMemberSearch();
            hookUI();
            syncGroupsListLifecycle();
            document.querySelectorAll(".nav-link[data-page]").forEach((btn) => {
              btn.addEventListener("click", () => {
                if (btn.dataset.page === "messages") setTimeout(syncGroupsListLifecycle, 0);
                else stopGroupsList();
              });
            });
          });

          window.addEventListener("telesyriana:user-changed", () => {
            applySupervisorVisibility();
            syncGroupsListLifecycle();
          });'''
          s = replace_required(s, old_tail, new_tail, 'groups hidden-page listener lifecycle')
          p.write_text(s)

          print('quota patches applied successfully')

      - name: Syntax check patched modules
        run: |
          cp app-core.js /tmp/app-core.mjs
          cp messages-core.js /tmp/messages-core.mjs
          cp groups-core.js /tmp/groups-core.mjs
          node --check /tmp/app-core.mjs
          node --check /tmp/messages-core.mjs
          node --check /tmp/groups-core.mjs

      - name: Commit patched stable cores
        run: |
          git config user.name 'TeleSyriana Quota Hotfix'
          git config user.email 'actions@users.noreply.github.com'
          git add app-core.js messages-core.js groups-core.js
          git commit -m '[quota-patch] reduce always-on Firestore listeners'
          git push origin HEAD:agent/firestore-quota-hotfix-v1
